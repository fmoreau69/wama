# REPRISE — 2026-08-11 · session « vérification + §10.1 + briques » — handoff pour session fraîche

> 🔴 **DÉPASSÉ (3 fois) — POINT D'ENTRÉE ACTUEL = `PROJECT_STATUS.md` §REPRISE
> « 2026-08-11→12 (3ᵉ-4ᵉ sessions, marches C + A1→A4 CLOSES, reste A5) »**, tout en haut des
> §REPRISE : harnais `app_regen_check` (marche C) + marches A1/A2/A3/A4 livrées et jugées
> CONFORMES, composition pilote B semée (19 manifestes), cible finale Translator/
> LibreTranslate. Suite = **marche A5** (models.py, juger sur transcriber). Le §0 ci-dessous
> (2ᵉ session) et les sections 1-5 (matin) sont l'HISTORIQUE de la journée du 11/08.

## 0. SESSION 2 (soir) — §10.3 LIVRÉ : le point d'entrée est ailleurs

> **Lire : `PROJECT_STATUS.md` §REPRISE « 2026-08-11 (2ᵉ session, SUITE du soir) »** puis le plan
> **C → A → B** dans `WAMA_APP_GENERATION_ROUTE.md §10.3` — c'est LA feuille de route suivante.

Résumé : **write-back 8 facettes** (`access` DB + 7 code via moteur commun marqué `[manifest-gen]`),
**passe INTÉGRÉE du pilote converter RÉUSSIE** (tout supprimé/régénéré en une fois : manifeste
8 facettes identiques, grille 93 % identique, smoke 200) ; triade **studio** dans la tool_api ;
**page librairies** `/model-manager/libraries/` + menu ; **`write_back_function`** → `UserFunction` ;
`WAMA_MANIFEST_ARCHITECTURE.md` remis au réel (4 kinds, §6quater) ; README à jour ; avis critique
consigné (trou #19 = détection de dérive nocturne).

**Suite (plan C→A→B, route §10.3)** : C = harnais `app_regen_check` (le juge) · A = gabarit
`processing` alimenté par le manifeste · B = rôle `codegen` wama-dev-ai (MISE À JOUR de
`prompts/dev.txt` sur le modèle `librarian.txt`) + banc de modèles (candidat `qwen3.6:35b`,
**MoE vérifié** 256 experts/8 actifs → offload tolérable) jugé par C ; pilote transcriber.

État : **tout committé ET poussé** (`dev` = `main` = origin). Le worktree bac à sable
`D:\WAMA\wt-regen-converter` (branche `regen/converter`) est conservé, propre, en retard de
quelques commits docs — le ff-merger depuis dev avant tout nouveau test destructif.

---

> Session mono-instance, périmètre large (imager, commun, studio, docs, infra). Détail des paliers
> imager : `REPRISE_2026-08-06_IMAGER.md` §0quinquies (+ MAJ). Ce fichier est le POINT D'ENTRÉE.

## 1. Ce qui est LIVRÉ (13 commits locaux, non poussés — `3ebf0d9` → `HEAD`)

| Palier | Commits | Contenu |
|---|---|---|
| Layout multi-files | `3ebf0d9` | bascules layout/design/pile sur TOUTES les files (imager + enhancer), validé Playwright |
| user_settings imager | `08f1665` | FAUX VERT réparé : écriture à la création (routeur unique), modèle legacy retiré (migr. 0016) |
| Purge imager | `ba9accb` | index.js 1655→660 lignes ; « Démarrer tout » était INOPÉRANT (réparé) ; toasts → brique |
| Start script durci | `d11e023`+`0c12a04` | 4 points `sudo` bloquants → `-n` (2 VÉCUS : hwclock 16 min, prlimit 8 min) |
| Critère user_settings | — | vert = `save_user_app_settings` ; anonymizer/enhancer → 🔶 honnêtes (modèle legacy) |
| **Route §10.1** | `b91f875` | `GENERIC_APPS` DÉRIVE ses E/S des ports (ordre=priorité) ; 3 `io_scope` déclarés ; describer `text`→`document` ; converter récupère `archive` |
| Doc-sync | — | 19 retards corrigés (ARCHITECTURE/SPEC/ROUTE/PROJECT_STATUS/VISION_STATUS) + 4 docstrings code menteurs |
| **Brique help_about** | — | onglets auto-générés d'APP_CATALOG (blocs par défaut) + routes /about/ /help/ = redirection vers l'onglet ; 12 classes locales mortes retirées ; 20/20 routes en 200 |
| Scories | — | stackdump supprimé ; `AI-models/models/vision/` gitignoré en bloc |

**Grille au départ de la prochaine session** : imager **93 %** (69/74), transcriber 94, converter/enhancer 93,
anonymizer/avatarizer 92, composer 88, reader 87, describer/synthesizer 84. `help_about` vert 10/10.

## 2. Leçons de session (à ne pas reperdre)

- **Playwright MCP** : réparé POUR DE VRAI (version épinglée `@0.0.79` + `--browser chromium` +
  build 1237). Vérifier par un VRAI appel navigateur, jamais par le statut. Cf. mémoire
  `reference_playwright_mcp`.
- **Ne JAMAIS éditer un script shell PENDANT qu'il s'exécute** (bash lit incrémentalement — parse
  corrompu vécu sur `start_wama_prod.sh`).
- **Un vert de grille se vérifie en EXÉCUTANT** (500 sur des pages « conformes », user_settings en
  lecture seule) — 3 critères durcis cette session pour exiger la vie, pas la présence.
- `rtk grep` a rendu des résultats faux/garblés 2× — utiliser l'outil Grep natif pour tout ce qui
  compte.

## 3. LA SUITE — §10.3 : bac à sable de régénération converter (acté avec Fabien)

Ordre acté : **converter → transcriber** (transcriber = composition complète `requires` → la
librairie faster-whisper ET **tous ses modèles** — le manifeste librairie ≠ modèle).

Démarche (route `WAMA_APP_GENERATION_ROUTE.md` §10.3, baseline déjà posée) :
1. `git worktree` = bac à sable ; ingest du manifeste `manifests/apps/converter.json` avec
   `resolve_requires`.
2. Faire grandir `write_back_app` **facette par facette** (aujourd'hui : `access` seule écrit ;
   9 facettes rapportées `codegen_required`). Commencer par les projections vers registres
   (identity→APP_CATALOG ?), garder idempotent/réversible/dry-run — le contrat des 3 write-backs
   existants.
3. Juger chaque incrément par le **diff régénéré/existant en artefacts NORMALISÉS** : manifeste
   ré-extrait identique (roundtrip niveau 2) + grille identique + smoke Playwright — JAMAIS un
   diff textuel byte à byte.
4. Chaque écart → 1 des 3 bacs : **déclarer** (manifeste), **porter** (`common/`), **trou de
   route** (consigner §11).

Pièges connus : ne pas créer de générateur parallèle (le maillon = `write_back` officiel des
kinds) ; pip install sous validation humaine ; AUCUN tirage de poids lourd (325 Go non joué) ;
`/studio/` exige une session connectée (validation navigateur limitée sans compte de test).

## 4. État infra en fin de session

- Stack COMPLET vérifié : gunicorn + 3 nœuds Celery + Redis + Postgres (relance par
  `start_wama_prod.sh`, désormais non-bloquant en non-interactif).
- 13 commits locaux **non poussés** (`dev` ahead 26 av. ce fichier) — push = demander à Fabien.
- Rien en attente côté fichiers : `git status` propre (hors ce handoff avant commit).

## 5. Points restant OUVERTS (hors périmètre de la suite immédiate)

- `user_settings` : reader + composer sans brique (rouges légitimes) ; anonymizer/enhancer sur
  modèle legacy (🔶) — à porter avec leurs apps.
- Angle mort récurrent de la grille : présence ≠ vie — durcir les critères au fil des découvertes.
- `during_preview` (émission 1/10), `recursive_import` (0/10, brique de route), `model_caps_ui`
  (1/10) : trous transverses, à traiter par la route pas par app.
- 3 points ouverts sauvegarde/tirage (session 10/08, périmètre disjoint) : inchangés.
